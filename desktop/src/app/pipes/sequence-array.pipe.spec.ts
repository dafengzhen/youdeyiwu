import { SequenceArrayPipe } from './sequence-array.pipe';

describe('SequenceArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SequenceArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
